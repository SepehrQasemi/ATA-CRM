services:
  crm-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ata-crm-web
    ports:
      - "3000:3000"
    env_file:
      - ./web/.env.local
    restart: unless-stopped
